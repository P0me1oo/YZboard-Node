# F:\xboard 工作区通用规则

本文件是 `F:\xboard` 工作区的长期通用规则，适用于当前及未来在此目录下工作的仓库。仓库自身更近层级的 `AGENTS.md`、项目说明和用户当前任务要求优先于本文件中的同类规则。

当前主要仓库包括：

- `F:\xboard\YZboard`
- `F:\xboard\YZboard-Node`
- `F:\xboard\YZ-Xray-core`

本文件只记录跨任务都成立的工作方式和测试环境边界。某一次任务的具体上游版本、协议、验收数字、临时凭据和一次性部署步骤必须放在该任务的提示词、Issue、计划或单独的任务文档中，不要不断追加到本文件。

## 通用工程规则

- 直接在用户指定的现有仓库和 fork 上工作；除非用户明确授权，不创建新 fork、不重命名仓库、不改变仓库归属。
- 不覆盖、回滚、删除或重新格式化用户已有的未提交修改。发现范围不明或用途不明的文件、服务、端口和数据时，先停下确认。
- 不把密码、Token、Cookie、UUID、私钥、订阅内容或其他认证信息写入源码、提交、通用文档、日志或最终报告。测试凭据必须使用专用值，并在任务结束时删除或轮换。
- 提交和 PR 不添加 AI 署名或其他 AI 使用痕迹。
- 先定位根因，再修改代码；结论、假设和环境限制要分开说明，不把未验证的判断写成事实。

## Fork、上游同步和版本规则

- 同步上游时使用固定的 Release Tag、分支快照或明确 commit。用于发布或部署的构建不得依赖持续变化的 `main`、`master` 或其他移动分支。
- 同步前记录上游仓库、基线引用、对应 commit、当前 fork commit 和本次变更范围；解决冲突时保留 fork 已有的用户功能和兼容代码。
- 不能把 fork 的修改伪装成上游版本，也不能覆盖上游 Tag。上游版本、fork 修订和产品版本分别记录。
- 优先遵循每个仓库已有的版本、Tag、Release、安装器和更新器约定。若项目没有明确约定，个人 fork 的可发布版本可以采用“上游版本 + fork patch”形式，例如 `v1.2.3-yz.1`、`v1.2.3-yz.2`；示例不是固定版本。
- 只做开发同步的提交可以使用 commit SHA；要上线或分发的新构建必须有可区分、可回滚的版本或构建标识。
- 不强行让多个项目使用同一个内部版本号。发布时用兼容矩阵记录各项目版本、上游依赖基线、fork commit 和构建校验值。
- 版本、Tag、Release、构建标识和变更说明需要保持一致，不要只改其中一项。除非当前任务明确要求，不修改与任务无关的依赖、替换关系或版本常量。

## 通用测试和构建规则

- 测试范围由当前任务定义；本文件不预设某一种协议、某一个 API 或某一组业务验收数字。
- 根据变更风险运行相关单元测试、集成测试、完整测试和目标平台构建。测试失败时记录准确命令、失败包、环境依赖和是否与本次代码有关。
- 不通过删除、跳过或弱化测试来制造绿色结果。固定外部网络、DNS、证书或服务导致的失败必须单独标注。
- 对依赖或构建链有变更时，检查最终产物的实际模块解析、VCS revision、目标操作系统/架构和 `vcs.modified` 状态。
- 对会影响流量、状态、配置、数据累计或重载的改动，至少验证正常路径、重复调用、重载/恢复和错误路径中的关键不变量。
- 测试、构建和临时文件应放在明确范围内；任务结束后清理不再需要的敏感数据和一次性文件。

## YZboard Docker 与 GHCR 发布流程

- `ghcr.io` 直接使用 GitHub 账号命名空间，不需要单独注册。YZboard 的标准镜像仓库为 `ghcr.io/p0me1oo/yzboard`。
- 优先使用 `YZboard/.github/workflows/docker-publish.yml` 在 GitHub Actions 中构建并发布镜像。工作流使用仓库自动提供的 `GITHUB_TOKEN` 和 `packages: write` 权限；禁止把 PAT、密码或 GHCR 登录信息写入仓库、工作流参数、Compose 或镜像。
- 发布来源必须是语义版本 Git Tag 或完整 commit SHA。手动触发工作流时，`source_ref` 禁止填写 `master`、`new-dev`、功能分支或其他移动引用；工作流和 Dockerfile 都必须核对最终源码 commit。
- Dockerfile 使用 `SOURCE_COMMIT` 获取固定源码。用于发布或生产部署的构建不得退回按 `BRANCH_NAME` clone 的方式，也不得在构建期间把 `config/app.php` 的语义版本临时改写为日期或 SHA。
- 每次发布必须生成 `linux/amd64` 和 `linux/arm64` manifest，并至少发布不可变标签 `<面板语义版本>-<短commit>`。面板语义版本别名用于按版本获取镜像；正式发布到生产环境的版本还必须同步更新 `latest`。
- YZboard 生产 Compose 长期使用 `ghcr.io/p0me1oo/yzboard:latest`，后续发版通过拉取并重建容器完成更新，不要求每次手工修改 `image:`。`latest` 只在完成测试并确认正式发布后更新；不可变标签和 digest 仍用于审计、版本确认和回滚。
- 发布后检查 GHCR 镜像名、完整来源 commit、OCI revision、manifest digest、`latest` 指向和两个目标架构，并把不可变镜像标签、完整 commit、digest 记录到 YZboard 兼容矩阵或相应发布记录。
- GHCR Package 是否设为 Public 属于发布配置。Public 镜像可以匿名 `docker pull`；Private 镜像要求服务器使用只含 `read:packages` 的凭据登录。修改可见性前必须明确确认，不自行扩大公开范围。

### 生产主控更新

- YZboard 生产服务器的 SSH 别名为 `DediRock-US`。每次更新仍须重新确认实际主机、部署目录、Compose 项目、服务名、当前镜像、持久化挂载和工作区修改；不能只凭历史记录直接操作。
- 生产部署目录保存的是 Compose、`.env`、数据库、主题、插件和其他持久化数据；应用代码来自 Docker 镜像。生产 Compose 的 `image:` 固定写为 `ghcr.io/p0me1oo/yzboard:latest`，后续更新不再修改该字段，也不在部署目录中强制 `git pull`、`git reset` 或重新 clone。
- 标准顺序是：确认 `latest` 对应的 GHCR manifest、来源 commit 和目标架构；备份 Compose 和必要的持久化数据；执行 `docker compose pull xboard`；运行 `docker compose config -q`；再执行 `docker compose up -d --no-build xboard`。容器不会因为远程 `latest` 变化而自动更新，必须完成拉取和重建。
- 容器启动后检查 Compose 状态、启动日志、面板 HTTP 健康、`config/app.php` 版本、镜像 OCI revision，并确认运行容器使用的镜像 digest 与本次发布的 `latest` 一致。入口程序正常执行 `xboard:update` 时，不重复执行安装流程。
- 保留更新前镜像 ID、不可变标签或 digest、原 Compose 和更新前数据备份作为回滚边界，不在确认新版本稳定前运行镜像清理。回滚时临时将 Compose 固定到上一版本的不可变标签或 digest，并同时考虑持久化数据兼容性，不能只改标签后就假定数据库一定兼容。
- 只有 GHCR 镜像尚未发布且用户明确同意时，才在生产服务器本地 `docker build`。本地构建仍须固定完整 commit、核对最终镜像 revision，并不得覆盖部署目录中的用户文件。

## 专用测试服务器总则

### Cloudnium-US

SSH 别名：`Cloudnium-US`

长期角色：被测服务器，用于运行隔离的服务端测试实例、测试构建和必要的临时控制面。

当前已知的既有资源包括：

- `nodeget-agent.service`
- Docker 容器 `panstar-checkin`
- 既有 SSH 服务、端口、防火墙规则、Docker 缓存、网络和卷

这些资源每次都必须重新确认，不能仅凭本文件中的历史记录假定状态。默认测试命名空间可以使用：

- 目录：`/opt/xboard-test`
- 服务端临时端口：优先选择空闲的专用端口，历史上使用过 `24443/udp`
- 本地测试控制面：优先绑定 `127.0.0.1`，历史上使用过 `18081/tcp`

端口和目录只是可复用约定，不是所有任务的固定验收条件。每次必须确认空闲并与既有服务隔离。不得使用生产节点身份、用户、凭据、证书或配置。

允许在 Cloudnium-US 这类被测 Node 服务器上部署隔离的 YZboard 测试面板或其他测试控制面，但这属于独立测试项目，不得与既有服务混用。此类部署必须使用独立的 compose/project 名称、数据目录、数据库/缓存、日志、监听端口、凭据和测试节点身份；默认只绑定本机或明确的测试范围。可以保留面板程序或镜像供下次复用，但测试面板中的测试节点、测试用户、机器身份、Token、证书、流量数据和生成配置仍属于一次性实例数据，按生命周期规则清理。

### 独角鲸-US

SSH 别名：`独角鲸-US`

长期角色：独立测试客户端，用于运行临时客户端、生成受控测试流量和验证跨服务器链路。

当前已知的既有资源包括：

- `narwhal-agent.service`
- `rfw.service`
- 所有既有 Podman 容器
- 默认系统代理、默认路由和无关监听端口

默认测试命名空间可以使用：

- 目录：`/opt/mihomo-xboard-test`
- 本地 HTTP/mixed 入口：历史上使用过 `127.0.0.1:19080`
- 本地 SOCKS 入口：历史上使用过 `127.0.0.1:19081`

客户端测试默认不启用 TUN，不修改系统代理、默认路由或无关端口。端口和目录只是可复用约定，使用前必须重新检查，不得覆盖既有客户端配置。

## 测试环境生命周期和复用

测试环境可以选择“保留复用”或“完全清理”，由当前任务和磁盘情况决定。

默认的“保留复用”模式：

- 可以保留明确测试目录中的已验证二进制、校验值、无敏感配置模板、目录结构和专用服务单元；
- 默认停止并禁用测试服务，不默认保持进程运行；
- 保留的是“程序层”安装，不是“实例层”数据。每次测试创建的 Node 实例、面板节点记录、测试用户、机器/节点身份、生成的 Xray/mihomo 配置、证书和密钥、数据库/缓存中的测试数据、流量累计、一次性文件和运行日志都必须停止使用并清理或重置；
- 删除或轮换测试密码、Token、UUID、私钥、订阅、证书私钥、完整可连接配置；
- 删除测试流量数据库、缓存、一次性上传文件和敏感日志；
- 删除临时公网防火墙规则和不再需要的监听端口；
- 下次使用前重新确认版本、二进制校验、配置隔离、测试身份、凭据和磁盘空间；
- 不把保留安装误认为保留运行状态。

Node 和 mihomo 的二进制、依赖、无敏感模板以及明确的专用服务单元可以保留，便于下次继续使用；但测试创建的节点实例和运行状态不能因为保留安装而自动保留。若保留 YZboard 测试面板，也只保留程序/镜像和经过清理的空白模板，不能保留上次测试的节点、用户、凭据或流量累计。

如果用户明确要求测试服务继续运行，必须在执行前确认，并在报告中记录服务、进程、端口、目录、暴露范围和停止方式。无法确认隔离范围时，停止服务并先说明。

“完全清理”模式只针对明确创建的测试目标，包括明确的服务单元、容器、网络、卷、镜像、目录和临时防火墙规则；不得扩大到生产资源或用途不明的路径。删除后要复核既有服务、容器、端口、路由、防火墙和磁盘状态。
